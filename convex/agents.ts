import { components } from "./_generated/api";

// @convex-dev/agent component is registered in convex.config.ts.
// Thread/message storage and streaming are used via aiActions + aiInternals.
export const agentComponent = components.agent;
